# Fixture spec: fixture-task

Static fixture spec document used only by `tests/run-shell-tests.sh` to exercise
`scripts/verify-guard-integrity.sh`'s spec-amend subcheck
(`docs/specs/guard-evasion-gates.md` Must-2). Not a real task spec.
