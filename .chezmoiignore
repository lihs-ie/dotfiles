# chezmoi が ~/ に apply しないファイル/ディレクトリ。
# dot_claude/ → ~/.claude/、dot_config/ → ~/.config/、dot_agents/ → ~/.agents/、
# plugins/ → ~/plugins/ を chezmoi 管理とし、
# repo root の dogfood ファイル (AGENTS.md / CLAUDE.md / scripts / ci 等) は repo 内のみ。

# dotfiles repo の dogfood (agent-policy-kit による自己適用)
AGENTS.md
CLAUDE.md
README.md
wiring_manifest.yml
scripts
ci
tests

# repo インフラ
.github
.gitignore
.chezmoiignore
**/__pycache__
**/*.pyc
