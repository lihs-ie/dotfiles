# ci/allowlist.yml — 本番パスの test double / 例外を期限付きで許可する。
# verify-no-prod-doubles.sh が path/expires_at を読み、期限内のものだけ検出から除外する。
# 無期限は禁止。期限切れエントリは CI を fail させる (scripts/verify-allowlist-expiry.sh)。
#
# entries:
#   - rule: no-prod-doubles
#     path: "applications/backend/src/Generated/**"   # glob
#     owner: "@lihs-ie"
#     reason: "protoc 生成コードに stub 命名が含まれるため"
#     expires_at: "2026-09-30"
entries: []
